apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluxcp.fullname" . }}-test-connection"
  labels:
    {{- include "fluxcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command:
        - wget
      args:
        - "--spider"
        - "--tries=3"
        - "--timeout=10"
        - "http://{{ include "fluxcp.fullname" . }}:{{ .Values.service.port }}/"
